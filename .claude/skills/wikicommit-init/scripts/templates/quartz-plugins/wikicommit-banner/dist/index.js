// src/i18n/locales/en-US.ts
var en_US_default = {
  components: {
    wikicommitBanner: {
      // Issue #774: the heading states a fact that is true of the page in
      // either state, so it no longer swaps. "Nobody has read this page yet"
      // (Issue #740) was false in front of the person reading it: what it
      // actually said was that no read by someone with write access had been
      // recorded, a distinction most readers do not hold. Dropping it is what
      // finally makes the heading obey Issue #739's principle — review adds a
      // line, it does not retract a warning — because a heading that swaps
      // forces the pending side to say *something*, and the only thing left to
      // say there is a negation. Issue #751 had to land first: with the
      // machine's own source check stated a line below, dropping the claim
      // about reading cannot be read as "nothing has been done to this page".
      title: "This page was generated by an LLM",
      // Split from the heading (Issue #774): the heading carries the fact, this
      // carries what follows from it. Before the split the two said nearly the
      // same thing.
      body: "It may contain inaccuracies.",
      generatedAt: "Generated:",
      generatedBy: "Model:",
      translatedAt: "Translated:",
      translatedBy: "Model:",
      // Issue #800: a sentence with a {name} placeholder rather than a bare
      // "label: value" pair, because the line now states what the reading
      // found as well as who did it. The renderer splits on {name} and puts
      // the profile link in the gap. `counts` in convert_wikilinks.py already
      // uses placeholders this way, so this is not a new pattern here.
      readBy: "Read by {name} \u2014 nothing obviously wrong stood out",
      // Issue #774: what `titleReviewed` used to be, moved out of the heading
      // and into the line review adds. It is shown only when `reviewed_by` is
      // absent — a route B page (`/wikicommit-review` runs locally and cannot
      // obtain a GitHub login) or one reviewed before that field existed. With
      // a name, the `readBy` line below says the same thing and says who,
      // and printing both would repeat "read" twice. Having this fallback is
      // what keeps the two states distinguishable without depending on
      // `reviewed_by` being present.
      readByAPerson: "A person read this page \u2014 nothing obviously wrong stood out",
      // Issue #751: what the machine check actually compared, stamped onto the
      // published copy of the page by convert_wikilinks.py and present on no
      // page in .wikicommit/entity/. Worded as "checked against its sources"
      // rather than "reviewed" or "verified": Pass 4 compares the page with the
      // documents it was written from and nothing else — completeness is
      // explicitly out of scope (Issue #722), and harm and conflicts with the
      // reader's own knowledge are seen by no layer at all (Issue #723).
      // Over-claiming here would repeat, in the opposite direction, the error
      // Issue #740 is correcting for `reviewed`.
      aiReviewAt: "Checked against sources:",
      aiReviewBy: "Checking model:",
      unknown: "unknown",
      reviewStatusLink: "Check this page's review status",
      reportLink: "Report something you noticed",
      // Issue #742: for a reader without a GitHub account this link lands on a
      // login wall — `/issues/new?...` redirects to `login?return_to=<the whole
      // URL>`, so the form is never shown. Issue #665 fixed the same shape of
      // error one layer up (the tracking Issue told readers to close an Issue
      // they have no permission to close); this is the layer below it. The
      // prefilled title and body do survive signing in, so the only thing left
      // to fix is that the link gives no warning — hence a statement of fact,
      // not a recruiting line. Kept out of the link label so the call to action
      // stays the loudest thing in the row (the visual-weight constraint this
      // shares with Issue #738).
      reportLinkAccountNote: "(GitHub account required)",
      reportTitlePrefix: "[Report]",
      reportBodyPage: "Page:",
      reportBodyLanguage: "Language:",
      reportBodyOriginal: "Original page:",
      // Issue #738: the two checklist items Issue #723 marked "only a person
      // can check this" reach only whoever browses the repository's Issue
      // list — in practice the operator. The reader most likely to notice the
      // harm item is usually the subject of the page, who never opens that
      // list but does read the page. This guidance rides the report link
      // instead, which every reader sees.
      reportBodyGuidanceHeading: "These are the kinds of problem no automated check can find. Write down any that apply (you are not being asked to go looking \u2014 only what you noticed while reading).",
      reportBodyGuidanceHarm: "- Anything about a real person or organization that felt overstated or too certain",
      reportBodyGuidanceKnowledge: "- Anything that conflicts with what you know (add a URL if you have one)",
      reportBodyGuidanceContradiction: "- Anything another page on this wiki says differently",
      // Not a closed list: a typo, a stale fact or a dead link is just as
      // welcome. Stated so the bullets above do not read as "report nothing
      // else".
      reportBodyGuidanceFooter: "Anything else is welcome too \u2014 typos, stale facts, broken links.",
      // The heading gives the reporter a marked place to write. `body=`
      // replaces report.md's own `## Page` / `## Problem` headings entirely, so
      // without this the prefilled body ends at the guidance and there is
      // nowhere obvious to type. The page facts above need no heading of their
      // own — each line already labels itself.
      reportBodyProblemHeading: "## Problem",
      siteSummaryPages: "Pages:",
      siteSummaryReviewed: "Read and checked by a person:",
      siteSummaryReviewNote: 'Every page is published as soon as an LLM generates it. The check against sources is run by machine; "read and checked by a person" is how many pages someone has since read all the way through without anything obviously wrong standing out. Only some pages are read by a person, by design \u2014 this number is not meant to reach the total, and it is not a complete quality guarantee.',
      siteSummaryAiReviewed: "Checked against sources:",
      siteSummaryAiReviewNote: '"Checked against sources" is how many pages were compared against their own sources when they were generated. That check covers agreement with those sources and nothing else \u2014 not completeness, not the effect on real people and organizations, not conflicts with what you know.'
    }
  }
};

// src/i18n/locales/ja-JP.ts
var ja_JP_default = {
  components: {
    wikicommitBanner: {
      // Issue #774: the heading states a fact that is true of the page in
      // either state, so it no longer swaps. "Nobody has read this page yet"
      // (Issue #740) was false in front of the person reading it: what it
      // actually said was that no read by someone with write access had been
      // recorded, a distinction most readers do not hold. Dropping it is what
      // finally makes the heading obey Issue #739's principle — review adds a
      // line, it does not retract a warning — because a heading that swaps
      // forces the pending side to say *something*, and the only thing left to
      // say there is a negation. Issue #751 had to land first: with the
      // machine's own source check stated a line below, dropping the claim
      // about reading cannot be read as "nothing has been done to this page".
      title: "LLM \u304C\u81EA\u52D5\u751F\u6210\u3057\u305F\u30DA\u30FC\u30B8\u3067\u3059",
      // Split from the heading (Issue #774): the heading carries the fact, this
      // carries what follows from it. Before the split the two said nearly the
      // same thing.
      body: "\u5185\u5BB9\u306B\u8AA4\u308A\u304C\u3042\u308B\u53EF\u80FD\u6027\u304C\u3042\u308A\u307E\u3059\u3002",
      generatedAt: "\u751F\u6210\u65E5:",
      generatedBy: "\u751F\u6210\u30E2\u30C7\u30EB:",
      translatedAt: "\u7FFB\u8A33\u65E5:",
      translatedBy: "\u7FFB\u8A33\u30E2\u30C7\u30EB:",
      // Issue #800: a sentence with a {name} placeholder rather than a bare
      // "label: value" pair, because the line now states what the reading
      // found as well as who did it. The renderer splits on {name} and puts
      // the profile link in the gap. `counts` in convert_wikilinks.py already
      // uses placeholders this way, so this is not a new pattern here.
      readBy: "{name} \u304C\u8AAD\u307F\u3001\u660E\u3089\u304B\u306A\u554F\u984C\u306F\u898B\u3064\u304B\u308A\u307E\u305B\u3093\u3067\u3057\u305F",
      // Issue #774: what `titleReviewed` used to be, moved out of the heading
      // and into the line review adds. It is shown only when `reviewed_by` is
      // absent — a route B page (`/wikicommit-review` runs locally and cannot
      // obtain a GitHub login) or one reviewed before that field existed. With
      // a name, the `readBy` line below says the same thing and says who,
      // and printing both would repeat "read" twice. Having this fallback is
      // what keeps the two states distinguishable without depending on
      // `reviewed_by` being present.
      readByAPerson: "\u4EBA\u304C\u8AAD\u307F\u3001\u660E\u3089\u304B\u306A\u554F\u984C\u306F\u898B\u3064\u304B\u308A\u307E\u305B\u3093\u3067\u3057\u305F",
      // Issue #751: what the machine check actually compared, stamped onto the
      // published copy of the page by convert_wikilinks.py and present on no
      // page in .wikicommit/entity/. Worded as "checked against its sources"
      // rather than "reviewed" or "verified": Pass 4 compares the page with the
      // documents it was written from and nothing else — completeness is
      // explicitly out of scope (Issue #722), and harm and conflicts with the
      // reader's own knowledge are seen by no layer at all (Issue #723).
      // Over-claiming here would repeat, in the opposite direction, the error
      // Issue #740 is correcting for `reviewed`.
      aiReviewAt: "\u51FA\u5178\u3068\u7167\u5408:",
      aiReviewBy: "\u7167\u5408\u30E2\u30C7\u30EB:",
      unknown: "\u4E0D\u660E",
      reviewStatusLink: "\u3053\u306E\u30DA\u30FC\u30B8\u306E\u30EC\u30D3\u30E5\u30FC\u72B6\u6CC1\u3092\u898B\u308B",
      reportLink: "\u6C17\u3065\u3044\u305F\u70B9\u3092\u5831\u544A\u3059\u308B",
      // Issue #742: for a reader without a GitHub account this link lands on a
      // login wall — `/issues/new?...` redirects to `login?return_to=<the whole
      // URL>`, so the form is never shown. Issue #665 fixed the same shape of
      // error one layer up (the tracking Issue told readers to close an Issue
      // they have no permission to close); this is the layer below it. The
      // prefilled title and body do survive signing in, so the only thing left
      // to fix is that the link gives no warning — hence a statement of fact,
      // not a recruiting line. Kept out of the link label so the call to action
      // stays the loudest thing in the row (the visual-weight constraint this
      // shares with Issue #738).
      reportLinkAccountNote: "\uFF08GitHub \u30A2\u30AB\u30A6\u30F3\u30C8\u304C\u5FC5\u8981\uFF09",
      reportTitlePrefix: "[\u5831\u544A]",
      reportBodyPage: "\u30DA\u30FC\u30B8:",
      reportBodyLanguage: "\u8A00\u8A9E:",
      reportBodyOriginal: "\u539F\u6587\u30DA\u30FC\u30B8:",
      // Issue #738: the two checklist items Issue #723 marked "only a person
      // can check this" reach only whoever browses the repository's Issue
      // list — in practice the operator. The reader most likely to notice the
      // harm item is usually the subject of the page, who never opens that
      // list but does read the page. This guidance rides the report link
      // instead, which every reader sees.
      reportBodyGuidanceHeading: "\u4EE5\u4E0B\u306F\u81EA\u52D5\u30C1\u30A7\u30C3\u30AF\u3067\u306F\u898B\u3064\u3051\u3089\u308C\u306A\u3044\u7A2E\u985E\u306E\u554F\u984C\u3067\u3059\u3002\u5F53\u3066\u306F\u307E\u308B\u3082\u306E\u304C\u3042\u308C\u3070\u66F8\u3044\u3066\u304F\u3060\u3055\u3044\uFF08\u63A2\u3057\u306B\u884C\u304F\u5FC5\u8981\u306F\u3042\u308A\u307E\u305B\u3093\u3002\u8AAD\u3093\u3067\u3044\u3066\u6C17\u3065\u3044\u305F\u3053\u3068\u3060\u3051\u3067\u69CB\u3044\u307E\u305B\u3093\uFF09\u3002",
      reportBodyGuidanceHarm: "- \u5B9F\u5728\u306E\u4EBA\u7269\u30FB\u7D44\u7E54\u306B\u3064\u3044\u3066\u3001\u66F8\u304D\u3059\u304E\u30FB\u65AD\u5B9A\u3057\u3059\u304E\u306B\u611F\u3058\u305F\u7B87\u6240",
      reportBodyGuidanceKnowledge: "- \u3042\u306A\u305F\u304C\u77E5\u3063\u3066\u3044\u308B\u3053\u3068\u3068\u98DF\u3044\u9055\u3046\u7B87\u6240\uFF08URL \u304C\u3042\u308C\u3070\u6DFB\u3048\u3066\u304F\u3060\u3055\u3044\uFF09",
      reportBodyGuidanceContradiction: "- \u4ED6\u306E\u30DA\u30FC\u30B8\u3068\u8A00\u3063\u3066\u3044\u308B\u3053\u3068\u304C\u9055\u3046\u3068\u611F\u3058\u305F\u70B9",
      // Not a closed list: a typo, a stale fact or a dead link is just as
      // welcome. Stated so the bullets above do not read as "report nothing
      // else".
      reportBodyGuidanceFooter: "\u4E0A\u8A18\u4EE5\u5916\uFF08\u8AA4\u5B57\u30FB\u53E4\u304F\u306A\u3063\u305F\u60C5\u5831\u30FB\u30EA\u30F3\u30AF\u5207\u308C\u306A\u3069\uFF09\u3082\u6B53\u8FCE\u3057\u307E\u3059\u3002",
      // The heading gives the reporter a marked place to write. `body=`
      // replaces report.md's own `## Page` / `## Problem` headings entirely, so
      // without this the prefilled body ends at the guidance and there is
      // nowhere obvious to type. The page facts above need no heading of their
      // own — each line already labels itself.
      reportBodyProblemHeading: "## \u5831\u544A\u5185\u5BB9",
      siteSummaryPages: "\u7DCF\u30DA\u30FC\u30B8\u6570:",
      siteSummaryReviewed: "\u4EBA\u304C\u8AAD\u3093\u3067\u78BA\u8A8D:",
      siteSummaryReviewNote: "\u30DA\u30FC\u30B8\u306F LLM \u304C\u751F\u6210\u3057\u305F\u6642\u70B9\u3067\u516C\u958B\u3055\u308C\u307E\u3059\u3002\u51FA\u5178\u3068\u306E\u7167\u5408\u306F\u6A5F\u68B0\u304C\u884C\u3044\u3001\u300C\u4EBA\u304C\u8AAD\u3093\u3067\u78BA\u8A8D\u300D\u306F\u305D\u306E\u3046\u3061\u4EBA\u304C\u6700\u5F8C\u307E\u3067\u8AAD\u307F\u3001\u660E\u3089\u304B\u306A\u554F\u984C\u3092\u898B\u3064\u3051\u306A\u304B\u3063\u305F\u4EF6\u6570\u3067\u3059\u3002\u4EBA\u306B\u3088\u308B\u78BA\u8A8D\u306F\u8A2D\u8A08\u4E0A\u4E00\u90E8\u306E\u30DA\u30FC\u30B8\u306E\u307F\u3067\u3042\u308A\u3001\u3053\u306E\u6570\u5B57\u304C\u7DCF\u6570\u306B\u9054\u3059\u308B\u3053\u3068\u306F\u76EE\u6307\u3057\u3066\u3044\u307E\u305B\u3093\u3002\u7DB2\u7F85\u7684\u306A\u54C1\u8CEA\u4FDD\u8A3C\u3067\u3082\u3042\u308A\u307E\u305B\u3093\u3002",
      siteSummaryAiReviewed: "\u51FA\u5178\u3068\u7167\u5408:",
      siteSummaryAiReviewNote: "\u300C\u51FA\u5178\u3068\u7167\u5408\u300D\u306F\u751F\u6210\u6642\u306B\u3001\u30DA\u30FC\u30B8\u306E\u8A18\u8FF0\u3092\u305D\u306E\u51FA\u5178\u3068\u7167\u5408\u3057\u305F\u4EF6\u6570\u3067\u3059\u3002\u7167\u5408\u3057\u3066\u3044\u308B\u306E\u306F\u51FA\u5178\u3068\u306E\u4E00\u81F4\u3060\u3051\u3067\u3001\u7DB2\u7F85\u6027\u30FB\u5B9F\u5728\u306E\u4EBA\u7269\u3084\u7D44\u7E54\u3078\u306E\u5F71\u97FF\u30FB\u8AAD\u8005\u81EA\u8EAB\u306E\u77E5\u8B58\u3068\u306E\u98DF\u3044\u9055\u3044\u306F\u898B\u3066\u3044\u307E\u305B\u3093\u3002"
    }
  }
};

// src/i18n/index.ts
var locales = {
  "en-US": en_US_default,
  "ja-JP": ja_JP_default
};
function i18n(locale) {
  return locales[locale] || en_US_default;
}
var LANG_TO_LOCALE = {
  en: "en-US",
  ja: "ja-JP"
};
function resolveLocale(frontmatterLang, cfgLocale) {
  if (typeof frontmatterLang === "string") {
    const mapped = LANG_TO_LOCALE[frontmatterLang];
    if (mapped) return mapped;
  }
  return cfgLocale ?? "en-US";
}

// src/components/styles/wikicommit-banner.scss
var wikicommit_banner_default = ".wikicommit-banner {\n  border: 1px solid var(--gray);\n  padding: 1rem 1.25rem;\n  margin-bottom: 1.5rem;\n  border-radius: 4px;\n  display: flex;\n  gap: 0.75rem;\n  align-items: flex-start;\n}\n\n.wikicommit-banner--pending {\n  border-left: 4px solid #f5a623;\n  background: #fffbf0;\n}\n\n:root[saved-theme=dark] .wikicommit-banner--pending {\n  background: #2a2618;\n  border-color: #6b5a1e;\n}\n\n.wikicommit-banner--reviewed {\n  border-left: 4px solid var(--gray);\n  background: var(--light);\n}\n\n.wikicommit-banner__icon {\n  font-size: 1.25rem;\n  flex-shrink: 0;\n}\n\n.wikicommit-banner__body p {\n  margin: 0.25rem 0 0;\n  font-size: 0.875rem;\n  color: var(--darkgray);\n}\n\n.wikicommit-banner__actions {\n  margin-top: 0.5rem;\n  display: flex;\n  flex-wrap: wrap;\n  gap: 0.75rem;\n}\n\n.wikicommit-banner__link {\n  color: var(--secondary);\n  text-decoration: underline;\n  font-size: 0.875rem;\n}\n\n.wikicommit-banner__report {\n  margin-bottom: 1.5rem;\n  font-size: 0.875rem;\n  display: flex;\n  flex-wrap: wrap;\n  gap: 0.75rem;\n}\n\n.wikicommit-banner__reviewer {\n  color: var(--darkgray);\n}\n\n.wikicommit-site-summary {\n  border: 1px solid var(--lightgray);\n  background: var(--light);\n  padding: 0.75rem 1rem;\n  margin-bottom: 1.5rem;\n  border-radius: 4px;\n}\n\n.wikicommit-site-summary__counts,\n.wikicommit-site-summary__note {\n  margin: 0;\n  font-size: 0.875rem;\n  color: var(--darkgray);\n}\n\n.wikicommit-site-summary__note {\n  margin-top: 0.375rem;\n  font-size: 0.8125rem;\n}\n\n.wikicommit-banner__report-action {\n  display: inline-flex;\n  flex-wrap: wrap;\n  align-items: baseline;\n  gap: 0.25rem;\n}\n\n.wikicommit-banner__link-note {\n  color: var(--darkgray);\n  font-size: 0.8125rem;\n}\n\n.wikicommit-banner__ai-review {\n  color: var(--darkgray);\n}";
var l;
function S(n2) {
  return n2.children;
}
l = { __e: function(n2, l2, u3, t2) {
  for (var i2, r2, o2; l2 = l2.__; ) if ((i2 = l2.__c) && !i2.__) try {
    if ((r2 = i2.constructor) && null != r2.getDerivedStateFromError && (i2.setState(r2.getDerivedStateFromError(n2)), o2 = i2.__d), null != i2.componentDidCatch && (i2.componentDidCatch(n2, t2 || {}), o2 = i2.__d), o2) return i2.__E = i2;
  } catch (l3) {
    n2 = l3;
  }
  throw n2;
} }, "function" == typeof Promise ? Promise.prototype.then.bind(Promise.resolve()) : setTimeout, Math.random().toString(8);

// node_modules/preact/jsx-runtime/dist/jsxRuntime.mjs
var f2 = 0;
function u2(e2, t2, n2, o2, i2, u3) {
  t2 || (t2 = {});
  var a2, c2, p2 = t2;
  if ("ref" in p2) for (c2 in p2 = {}, t2) "ref" == c2 ? a2 = t2[c2] : p2[c2] = t2[c2];
  var l2 = { type: e2, props: p2, key: n2, ref: a2, __k: null, __: null, __b: 0, __e: null, __c: null, constructor: void 0, __v: --f2, __i: -1, __u: 0, __source: i2, __self: u3 };
  if ("function" == typeof e2 && (a2 = e2.defaultProps)) for (c2 in a2) void 0 === p2[c2] && (p2[c2] = a2[c2]);
  return l.vnode && l.vnode(l2), l2;
}

// src/components/WikiCommitBanner.tsx
function entityPathToRelativePath(entityPath) {
  return entityPath.trim().replace(/^\.\//, "").replace(/^\.wikicommit\/(entity|wiki)\//, "").replace(/^([^/]+)\/custom\//, "$1/");
}
function buildPageUrl(baseUrl, slug) {
  return baseUrl && slug ? `https://${baseUrl.replace(/\/+$/, "")}/${slug}` : void 0;
}
function resolveOriginalPageInfo(frontmatter, allFiles, cfg) {
  const translatedFrom = frontmatter?.translated_from;
  if (typeof translatedFrom !== "string") return void 0;
  const parentRelativePath = entityPathToRelativePath(translatedFrom);
  const parent = allFiles.find((f3) => f3.relativePath === parentRelativePath);
  if (parent?.frontmatter?.status === "removed") return translatedFrom;
  return buildPageUrl(cfg?.baseUrl, parent?.slug) ?? translatedFrom;
}
var REPORT_NOTE_ID = "wikicommit-banner-report-note";
var AI_REVIEW_AT_FIELD = "ai_review_at";
var AI_REVIEW_MODEL_FIELD = "ai_review_model";
function buildReviewSearchUrl(repo, type, lang, relativePath) {
  if (!repo || !type || !lang || !relativePath) return void 0;
  const slug = relativePath.split("/").pop()?.replace(/\.md$/, "");
  if (!slug) return void 0;
  const q2 = `is:issue is:open label:wikicommit-review in:title "${type}/${slug} (${lang})"`;
  return `https://github.com/${repo}/issues?q=${encodeURIComponent(q2)}`;
}
function renderReadBy(template, login) {
  const [before, ...rest] = template.split("{name}");
  const after = rest.join("{name}");
  return /* @__PURE__ */ u2(S, { children: [
    before,
    /* @__PURE__ */ u2("a", { href: `https://github.com/${encodeURIComponent(login)}`, children: login }),
    after
  ] });
}
var WikiCommitBanner = ({ fileData, allFiles, cfg }) => {
  const frontmatter = fileData.frontmatter;
  if (frontmatter?.status === "removed") return null;
  const reviewStatus = frontmatter?.review_status ?? "pending";
  const isPending = reviewStatus === "pending";
  const t2 = i18n(resolveLocale(frontmatter?.lang, cfg?.locale)).components.wikicommitBanner;
  const pageCount = frontmatter?.wikicommit_page_count;
  const reviewedCount = frontmatter?.wikicommit_reviewed_count;
  const aiReviewedCount = frontmatter?.wikicommit_ai_reviewed_count;
  const siteSummary = typeof pageCount === "number" && typeof reviewedCount === "number" ? /* @__PURE__ */ u2("div", { class: "wikicommit-site-summary", children: [
    /* @__PURE__ */ u2("p", { class: "wikicommit-site-summary__counts", children: [
      t2.siteSummaryPages,
      " ",
      /* @__PURE__ */ u2("strong", { children: pageCount }),
      "\xA0\xA0",
      typeof aiReviewedCount === "number" ? /* @__PURE__ */ u2(S, { children: [
        t2.siteSummaryAiReviewed,
        " ",
        /* @__PURE__ */ u2("strong", { children: aiReviewedCount }),
        "\xA0\xA0"
      ] }) : null,
      t2.siteSummaryReviewed,
      " ",
      /* @__PURE__ */ u2("strong", { children: reviewedCount })
    ] }),
    /* @__PURE__ */ u2("p", { class: "wikicommit-site-summary__note", children: t2.siteSummaryReviewNote }),
    typeof aiReviewedCount === "number" ? /* @__PURE__ */ u2("p", { class: "wikicommit-site-summary__note", children: t2.siteSummaryAiReviewNote }) : null
  ] }) : null;
  const isTranslation = typeof frontmatter?.translated_from === "string";
  const generatedAt = isTranslation ? frontmatter?.translated_at ?? t2.unknown : frontmatter?.generated_at ?? t2.unknown;
  const generatedBy = isTranslation ? frontmatter?.translated_by ?? t2.unknown : frontmatter?.generated_by ?? t2.unknown;
  const generatedAtLabel = isTranslation ? t2.translatedAt : t2.generatedAt;
  const generatedByLabel = isTranslation ? t2.translatedBy : t2.generatedBy;
  const aiReviewAt = frontmatter?.[AI_REVIEW_AT_FIELD];
  const aiReviewBy = frontmatter?.[AI_REVIEW_MODEL_FIELD];
  const aiReviewLine = typeof aiReviewAt === "string" && aiReviewAt.trim() !== "" && typeof aiReviewBy === "string" && aiReviewBy.trim() !== "" ? /* @__PURE__ */ u2("p", { class: "wikicommit-banner__ai-review", children: [
    t2.aiReviewAt,
    " ",
    aiReviewAt,
    "\xA0\xA0",
    t2.aiReviewBy,
    " ",
    aiReviewBy
  ] }) : null;
  const repo = process.env.GITHUB_REPOSITORY;
  const title = frontmatter?.title ?? "";
  const type = typeof frontmatter?.type === "string" ? frontmatter.type.replace(/^schema:/, "") : void 0;
  const lang = frontmatter?.lang;
  const pageUrl = buildPageUrl(cfg?.baseUrl, fileData.slug);
  const originalPageInfo = isTranslation ? resolveOriginalPageInfo(frontmatter, allFiles, cfg) : void 0;
  const reportTitle = type ? `${t2.reportTitlePrefix} ${type}: ${title}` : `${t2.reportTitlePrefix} ${title}`;
  const reportFacts = [
    pageUrl ? `${t2.reportBodyPage} ${pageUrl}` : null,
    lang ? `${t2.reportBodyLanguage} ${lang}` : null,
    originalPageInfo ? `${t2.reportBodyOriginal} ${originalPageInfo}` : null
  ].filter((line) => line !== null);
  const reportGuidance = [
    t2.reportBodyProblemHeading,
    "",
    "<!--",
    t2.reportBodyGuidanceHeading,
    "",
    t2.reportBodyGuidanceHarm,
    t2.reportBodyGuidanceKnowledge,
    t2.reportBodyGuidanceContradiction,
    "",
    t2.reportBodyGuidanceFooter,
    "-->",
    ""
  ];
  const reportBody = [...reportFacts, ...reportFacts.length ? [""] : [], ...reportGuidance].join(
    "\n"
  );
  const reportUrl = repo ? `https://github.com/${repo}/issues/new?template=report.md&title=${encodeURIComponent(reportTitle)}${reportBody ? `&body=${encodeURIComponent(reportBody)}` : ""}` : "#";
  const reportAction = /* @__PURE__ */ u2("span", { class: "wikicommit-banner__report-action", children: [
    /* @__PURE__ */ u2("a", { href: reportUrl, class: "wikicommit-banner__link", "aria-describedby": REPORT_NOTE_ID, children: t2.reportLink }),
    /* @__PURE__ */ u2("span", { class: "wikicommit-banner__link-note", id: REPORT_NOTE_ID, children: t2.reportLinkAccountNote })
  ] });
  const reviewedBy = typeof frontmatter?.reviewed_by === "string" && frontmatter.reviewed_by.trim() !== "" ? frontmatter.reviewed_by.trim() : void 0;
  const stampFields = isTranslation ? ["translated_at", "translated_by"] : ["generated_at", "generated_by"];
  const hasGenerationStamp = stampFields.some(
    (field) => typeof frontmatter?.[field] === "string" && frontmatter[field].trim() !== ""
  );
  if (!isPending && !hasGenerationStamp) {
    return /* @__PURE__ */ u2(S, { children: [
      siteSummary,
      /* @__PURE__ */ u2("div", { class: "wikicommit-banner__report", children: [
        reviewedBy ? /* @__PURE__ */ u2("span", { class: "wikicommit-banner__reviewer", children: renderReadBy(t2.readBy, reviewedBy) }) : null,
        reportAction
      ] })
    ] });
  }
  const reviewSearchUrl = isPending ? buildReviewSearchUrl(repo, type, lang, fileData.relativePath) : void 0;
  return /* @__PURE__ */ u2(S, { children: [
    siteSummary,
    /* @__PURE__ */ u2(
      "div",
      {
        class: `wikicommit-banner ${isPending ? "wikicommit-banner--pending" : "wikicommit-banner--reviewed"}`,
        children: [
          /* @__PURE__ */ u2("span", { class: "wikicommit-banner__icon", children: "\u26A0\uFE0F" }),
          /* @__PURE__ */ u2("div", { class: "wikicommit-banner__body", children: [
            /* @__PURE__ */ u2("strong", { children: t2.title }),
            /* @__PURE__ */ u2("p", { children: t2.body }),
            /* @__PURE__ */ u2("p", { children: [
              generatedAtLabel,
              " ",
              generatedAt,
              "\xA0\xA0",
              generatedByLabel,
              " ",
              generatedBy
            ] }),
            aiReviewLine,
            !isPending ? /* @__PURE__ */ u2("p", { class: "wikicommit-banner__reviewer", children: reviewedBy ? renderReadBy(t2.readBy, reviewedBy) : t2.readByAPerson }) : null,
            /* @__PURE__ */ u2("div", { class: "wikicommit-banner__actions", children: [
              reviewSearchUrl ? /* @__PURE__ */ u2("a", { href: reviewSearchUrl, class: "wikicommit-banner__link", children: t2.reviewStatusLink }) : null,
              reportAction
            ] })
          ] })
        ]
      }
    )
  ] });
};
WikiCommitBanner.css = wikicommit_banner_default;
var WikiCommitBanner_default = (() => WikiCommitBanner);

export { WikiCommitBanner_default as WikiCommitBanner };
//# sourceMappingURL=index.js.map
//# sourceMappingURL=index.js.map